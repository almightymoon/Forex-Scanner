global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: fxnav-market-data
    metrics_path: /market/metrics
    static_configs:
      - targets: ["host.docker.internal:8001"]
        labels:
          service: fxnav-collector
