"""Shim — use swing_engine.config."""

from swing_engine.config import get_config, get_swing_detection_config
from swing_engine.utils import compute_atr_series, atr_at, log_stage, pip_size_for_symbol, pips_to_price
